<?xml version="1.0" ?>
<UserControl 
  x:Class="HisouSangokushiZero2_1_Uno.Pages.ExInfo"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="using:HisouSangokushiZero2_1_Uno.Pages"
  xmlns:code="using:HisouSangokushiZero2_1_Uno.Code">
  <Grid x:Name="Content" Background="#CEEF">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition/>
    </Grid.RowDefinitions>
    <Grid x:Name="InfoButtonsPanel" Height="{x:Bind code:UIUtil.infoButtonHeight}">
      <Grid.ColumnDefinitions>
        <ColumnDefinition/>
        <ColumnDefinition/>
        <ColumnDefinition/>
        <ColumnDefinition/>
        <ColumnDefinition/>
      </Grid.ColumnDefinitions>
      <Button Grid.Column="0" x:Name="ExplainButton" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#AFFF" BorderBrush="Black" BorderThickness="2" Padding="0">
        <TextBlock Text="ƒ‹[ƒ‹Ú×"/>
      </Button>
      <Button Grid.Column="1" x:Name="WinCondButton" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#AFFF" BorderBrush="Black" BorderThickness="2" Padding="0">
        <TextBlock Text="Šew‰c‚ÌŸ—˜ðŒ"/>
      </Button>
      <Button Grid.Column="2" x:Name="ParamListButton" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#AFFF" BorderBrush="Black" BorderThickness="2" Padding="0">
        <TextBlock Text="l•¨Ew‰cƒf[ƒ^"/>
      </Button>
      <Button Grid.Column="3" x:Name="ProductionInfoButton" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#AFFF" BorderBrush="Black" BorderThickness="2" Padding="0">
        <TextBlock Text=" §ìî•ñ"/>
      </Button>
      <Button Grid.Column="4" x:Name="SettingButton" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#AFFF" BorderBrush="Black" BorderThickness="2" Padding="0">
        <TextBlock Text="Ý’è"/>
      </Button>
    </Grid>
    <Grid x:Name="InfoContentPanel" Grid.Row="1" Grid.ColumnSpan="5"/>
  </Grid>
</UserControl>