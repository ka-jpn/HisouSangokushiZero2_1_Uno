<?xml version="1.0" ?>
<UserControl 
  x:Class="HisouSangokushiZero2_1_Uno.Pages.SaveAndLoad"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:local="using:HisouSangokushiZero2_1_Uno.Pages">
  <Grid x:Name="Content" Background="#EEE" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <TextBlock x:Name="Title" FontSize="40" LineHeight="46" HorizontalAlignment="Center"/>
    <ListView x:Name="Scroll" Grid.Row="1" MaxWidth="{x:Bind local:SaveAndLoad.scrollMaxWidth}" HorizontalAlignment="Stretch" ItemsSource="{x:Bind local:SaveAndLoad.saveSlotTexts,Mode=OneWay}">
      <ListView.ItemTemplate>
        <DataTemplate x:DataType="local:SaveSlotData">
          <StackPanel Orientation="Horizontal" Background="#FFF" Margin="2" PointerPressed="SaveSlot_PointerPressed">
            <TextBlock Text="{x:Bind local:SaveAndLoad.IndexToFileNo(SlotIndex)}" FontSize="36" LineHeight="40" Width="60" TextAlignment="Center" VerticalAlignment="Center"/>
            <StackPanel Margin="0,0,-50,0" VerticalAlignment="Center">
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="{x:Bind NowScenario}" Margin="10,0"/>
                <TextBlock Text="{x:Bind PlayCountry}" Margin="10,0"/>
                <TextBlock Text="{x:Bind PlayTurn}" Margin="10,0"/>
              </StackPanel>
              <StackPanel Orientation="Horizontal">
                <TextBlock Text="{x:Bind PlayTime}" Margin="10,0"/>
                <TextBlock Text="{x:Bind LastSaveDate}" Margin="10,0"/>
                <TextBlock Text="{x:Bind GameVersion}" Margin="10,0"/>
              </StackPanel>
            </StackPanel>
            <TextBlock Text="{x:Bind HasSaveData}" VerticalAlignment="Center"/>
          </StackPanel>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
    <Button x:Name="CloseButton" Grid.Row="2" MaxWidth="350" Height="60" Margin="5" HorizontalAlignment="Stretch">
      <TextBlock Text="‚à‚Ç‚é"/>
    </Button>
  </Grid>
</UserControl>